<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Página Listar Produtos</title>
</head>
<body>
    <h2>Lista de Produtos cadastrados</h2>

<table border="1">
    <tr>
        <th>ID</th>
        <th>Nome</th>
        <th>preço</th>
        <th>categoria</th>
        <th>Ações</th>
    </tr>

    <tr th:each="prod : ${produtosAchados}">
        <td th:text="${prod.id}"></td>
        <td th:text="${prod.nome}"></td>
        <td th:text="${prod.preco}"></td>
        <td th:text="${prod.categoria}"></td>
        <td>
                <a th:href="@{/produtoCTR//{id}(id=${prod.id})}">Editar</a>
                    
                <a th:href="@{/produtoCTR//{id}(id=${prod.id})}"
                onclick="return confirm('Deseja realmente excluir este produto?')">Excluir</a>
        </td>
    </tr>

</table>

<br>
<a th:href="@{/produtoCTR/formCadastrarProd}">Novo Produto</a>

</body>
</html>

----------------------------------------------------------------------

<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Editar Produto</title>
</head>
<body>
    <h1>Editar Empresa</h1>

    <form th:action="@{'/produtoCTR//'+${produtoEditar.id}}"
    th:object="${produtoEditar}" method="post">

        <label for="nome">Nome do Produto:</label>
        <input type="text" id="nome" th:field="*{nome}" required>
        <br><br>

        <label for="preco">Preço:</label>
        <input type="number" id="preco" th:field="*{preco}" required>
        <br><br>

        <label for="categoria">Categoria:</label>
        <input type="text" id="categoria" th:field="*{categoria}" required>
        <br><br>

        <button type="submit">Salvar Alterações</button>
    </form>

    <br>
    <a th:href="@{/produtoCTR/}">Voltar à Listagem de Produtos</a>
</body>
</html>